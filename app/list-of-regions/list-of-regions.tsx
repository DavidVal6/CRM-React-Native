import { ScrollView, Text } from "react-native";

export default function listOfRegions() {
    return(
        <ScrollView>
            <Text>List of listOfRegions</Text>
        </ScrollView>
    );
}